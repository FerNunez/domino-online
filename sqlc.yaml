version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries/*.sql"
    schema: "sql/schemas/*.sql"
    gen:
      go:
        package: "sql"
        sql_package: "pgx/v5"
        out: "shared/db/sql"
